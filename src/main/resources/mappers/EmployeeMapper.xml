<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.yi.gradle_mybatis_c3p0.dao.EmployeeDao">

	<select id="selectEmployeeByAll" resultType="Employee">
		select eno, ename, salary, dno, gender, joindate, title from employee
	</select>
	
	<insert id="insertEmployee" parameterType="Employee">
		insert into employee(eno, ename, salary, dno, gender, joindate, title) values(#{eno}, #{ename}, #{salary}, #{dno}, #{gender}, #{joindate}, #{title})
	</insert>
	
	<delete id="deleteEmployee" parameterType="Employee">
		delete from employee where eno = #{eno}
	</delete>
	
	<update id="updateEmployee" parameterType="Employee">
		update employee
		set ename = #{ename}, salary = #{salary}, dno= #{dno}, gender= #{gender}, joindate= #{joindate}, title = #{title}
		where eno = #{eno}
	</update>
	
	<select id="selectEmployeeByCode" resultType="Employee">
		select eno, ename, salary, dno, gender, joindate, title 
		from employee
		where eno = #{eno}
	</select>
</mapper>